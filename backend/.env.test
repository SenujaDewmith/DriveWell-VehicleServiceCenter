DB_USER=postgres
DB_HOST=localhost
DB_NAME=vehicle_service_test
DB_PASSWORD=root
DB_PORT=5433

JWT_SECRET=wenfkwefkismfdskd

# Email (MailHog local SMTP — run: mailhog.exe / docker start mailhog)
SMTP_HOST=localhost
SMTP_PORT=1025
EMAIL_FROM="DriveWell Test" <no-reply@drivewell.local>
DATABASE_URL="postgresql://postgres:root@localhost:5433/vehicle_service_test"
NODE_ENV=test
